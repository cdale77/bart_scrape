version: 2
jobs:
  build:
    working_directory: ~/bart_scrape
    docker:
      - image: cdale77/docker-elixir-base:latest
      - image: postgres:9.6
        environment:
          POSTGRES_USER: postgres
    steps:
      - checkout
      - run: mix deps.get
      - run: mix ecto.create
      - run: mix test
