version: 2
jobs:
  build:
    working_directory: ~/bart_scrape
    docker:
      - image: circleci/elixir:1.5
      - image: postgres:9.6
        environment:
          POSTGRES_USER: postgres
    steps:
      - checkout
      - run: mix deps.get
      - run: mix ecto.create
      - run: mix test
